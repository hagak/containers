---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["main"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: hagak/containers/.github/workflows/simple-checks.yaml@b09044bd781d5d51ec9267ea2e978e36b3b7a78d # main

  get-changed-images:
    uses: hagak/containers/.github/workflows/get-changed-images.yaml@b09044bd781d5d51ec9267ea2e978e36b3b7a78d # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: hagak/containers/.github/workflows/build-images.yaml@b09044bd781d5d51ec9267ea2e978e36b3b7a78d # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
