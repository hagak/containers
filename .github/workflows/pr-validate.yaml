---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["main"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: hagak/containers/.github/workflows/simple-checks.yaml@7b011a7fecc7aab1c213d6fc0ad81f2fa4116972 # main

  get-changed-images:
    uses: hagak/containers/.github/workflows/get-changed-images.yaml@7b011a7fecc7aab1c213d6fc0ad81f2fa4116972 # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: hagak/containers/.github/workflows/build-images.yaml@7b011a7fecc7aab1c213d6fc0ad81f2fa4116972 # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
