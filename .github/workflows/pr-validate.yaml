---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["main"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: hagak/containers/.github/workflows/simple-checks.yaml@ed7e8619114c64b26f98b3f09a71c7fee7737d22 # main

  get-changed-images:
    uses: hagak/containers/.github/workflows/get-changed-images.yaml@ed7e8619114c64b26f98b3f09a71c7fee7737d22 # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: hagak/containers/.github/workflows/build-images.yaml@ed7e8619114c64b26f98b3f09a71c7fee7737d22 # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
