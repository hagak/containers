---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["main"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: hagak/containers/.github/workflows/simple-checks.yaml@2216be54fa3f77008188ad3c368355b8056929a9 # main

  get-changed-images:
    uses: hagak/containers/.github/workflows/get-changed-images.yaml@2216be54fa3f77008188ad3c368355b8056929a9 # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: hagak/containers/.github/workflows/build-images.yaml@2216be54fa3f77008188ad3c368355b8056929a9 # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
